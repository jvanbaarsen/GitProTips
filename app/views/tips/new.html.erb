<div class="row">
  <div class="page-header">
    <h1>Submit your own awesome Git Tip</h1>
  </div>
  <div class="alert alert-info">
  <p>At this point every tip has to be accepted manually, so it might take a little time for you tip to appear on the website.</p>
  <p>We are working super hard to create a realtime submission system, so please bear with us!</p>
</div>
</div>
<div class="row">
<%= form_for @tip, url: new_tip_path, html: {class: 'form-vertical'} do |f| %>
  <div class="form-group <%= "has-error" if @tip.errors[:title].present? %>">
    <%= f.label :title, class: 'control-label' %>
    <%= f.text_field :title, class: 'form-control', placeholder: 'Title of your tip' %>
  </div>
  <div class="form-group <%= "has-error" if @tip.errors[:content].present? %>">
    <%= f.label :content, class: 'control-label' %>
      <%= f.text_area :content, class: 'form-control', placeholder: 'Your awesome tip', rows: 13 %>
  </div>
  <div class="form-group">
    <%= link_to "Go back", root_path %>
      <%= f.submit "Submit tip", class: 'btn btn-success pull-right' %>
  </div>
<% end %>
  </div>
